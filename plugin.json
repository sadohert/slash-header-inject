{
    "id": "slash-header-inject",
    "name": "Custom Header Injection",
    "description": "This plugin injects custom HTTP headers prior to making slash command API endpoint calls.  This is a reference only.  Changing the headers requires editing and rebuilding the code (for now...)",
    "version": "0.0.1",
    "server": {
        "executables": {
            "linux-amd64": "server/dist/plugin-linux-amd64",
            "darwin-amd64": "server/dist/plugin-darwin-amd64",
            "windows-amd64": "server/dist/plugin-windows-amd64.exe"
        }
    },
    "webapp": {
        "bundle_path": "webapp/dist/main.js"
    },
    "settings_schema": {
        "header": "",
        "footer": "",
        "settings": []
    }
}
